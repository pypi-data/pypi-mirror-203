[tool.poetry]
name = "databutton-logger"
version = "0.9.2"
description = "Logger utilities for databutton"
authors = ["Databutton"]
packages = [
  { include = "databutton_logger"}
]

[tool.poetry.dependencies]
python = "^3.9"
asgi-correlation-id = "^4.1.0"
fastapi = ">=0.80"
loguru = "^0.7.0"
httpx = "^0.24.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.1.3"
black = "^22.8.0"
isort = "^5.10.1"
flake8 = "^5.0.4"
python-semantic-release = "^7.31.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.isort]
profile = "black"

[tool.semantic_release]
version_variable = "databutton_logger/__init__.py:__version__"
version_toml = "pyproject.toml:tool.poetry.version"
major_on_zero = false
branch = "main"
upload_to_pypi = true
upload_to_release = true
build_command = "poetry build"
