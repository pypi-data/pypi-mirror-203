[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auraboros"
version = "0.11.0-alpha"
dependencies = ["pygame"]
requires-python = ">=3.10.8"

[tool.hatch.build]
# exclude = ["as"]
