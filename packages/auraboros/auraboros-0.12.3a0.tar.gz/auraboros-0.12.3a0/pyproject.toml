[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "auraboros"
version = "0.12.3-alpha"
dependencies = ["pygame", "moderngl"]
requires-python = ">=3.10.8"

[tool.hatch.build]
# exclude = ["as"]
