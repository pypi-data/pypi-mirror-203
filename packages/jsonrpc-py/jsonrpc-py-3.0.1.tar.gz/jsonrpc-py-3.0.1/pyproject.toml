[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line_length = 140
preview = true

[tool.isort]
profile = "black"
combine_as_imports = true
line_length = 140
src_paths = ["jsonrpc", "tests"]

[tool.mypy]
show_column_numbers = true
show_error_codes = true
strict = true

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true
