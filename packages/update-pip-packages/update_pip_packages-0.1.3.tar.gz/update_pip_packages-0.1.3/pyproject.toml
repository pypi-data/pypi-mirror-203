[tool.poetry]
name = "update-pip-packages"
version = "0.1.3"
description = ""
authors = ["OleksandrMakarov"]
readme = "README.md"
packages = [{include = "update_project"}]

[tool.poetry.dependencies]
python = "^3.10"
distro = "^1.8.0"

[tool.poetry.scripts]
update-pip-packages = "update_project.update_packages:main"


[tool.poetry.group.dev.dependencies]
pytest = "^7.3.1"
autopep8 = "^2.0.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
