coverage:
  extends:
    - .base
    - .test_base
  needs:
    - lint
  script:
    - make coverage_xml
  coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
