from setuptools import setup, find_packages

VERSION = '1.0'
DESCRIPTION = 'Streaming video data via networks'

setup(
    name="Planes2D",
    version=VERSION,
    author="KomornikBank0",
    description=DESCRIPTION,
    packages=find_packages(),
)
