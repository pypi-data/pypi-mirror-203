[tool.poetry]
name = "neet-cli"
version = "0.1.0"
description = ""
authors = ["Eliel <eliel.hj90@gmail.com>"]
readme = "README.md"
packages = [{include = "neet_cli"}]

[tool.poetry.scripts]
neet-cli = "neet_cli.main:app"

[tool.poetry.dependencies]
python = "^3.11"
typer = {extras = ["all"], version = "^0.7.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
