[tool.poetry]
name = "flasking"
version = "0.0.0"
description = "A enhanced way of using flask."
authors = ["ANDREI12333"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
