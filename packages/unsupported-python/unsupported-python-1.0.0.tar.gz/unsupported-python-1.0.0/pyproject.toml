[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unsupported-python"
version = "1.0.0"
maintainers = [
    {name = "Nicolas Tessore", email = "n.tessore@ucl.ac.uk"},
]
description = "Conditional dependency to indicate unsupported Python versions"
readme = "README.md"
license = {text = "MIT"}

[project.urls]
Homepage = "https://github.com/ntessore/unsupported-python"

[tool.setuptools]
packages = []
